# C++ Project Setup

This is a C++ project using CMake build system.

- Verify project structure is in place
- All C++ source files are in `src/`
- Header files are in `include/`
- Build files are generated in `build/`
